[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m uvicorn app.main_judge_ready:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/healthz"
healthcheckTimeout = 100

[env]
NODE_VERSION = "18"
